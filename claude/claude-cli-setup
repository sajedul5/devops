mkdir -p ~/.local/bin ~/.claude-profiles/verbex-ai && printf '%s\n' '#!/bin/sh' 'export CLAUDE_CONFIG_DIR="$HOME/.claude-profiles/verbex-ai"' 'exec claude "$@"' > ~/.local/bin/claude-verbex-ai && chmod +x ~/.local/bin/claude-verbex-ai && echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc && echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/.local/bin:$PATH"