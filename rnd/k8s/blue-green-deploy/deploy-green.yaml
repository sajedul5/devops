apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-green
  namespace: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
      version: green
  template:
    metadata:
      labels:
        app: frontend
        version: green
    spec:
      containers:
      - name: frontend
        image: sajedul5/devops:frontend-green
        ports:
        - containerPort: 3000
        env:
        - name: API_URL
          value: "http://api.local"